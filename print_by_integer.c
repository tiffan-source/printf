#include "main.h"

/**
 * print_number - print an integer
 * @n: integer to print
 *
 * Return: Number of charactere print
 */

int print_number(int n)
{
	/* unsigned int number; */

	/* if (n < 0) */
	/* { */
	/* 	print_char('-'); */
	/* 	return (1 + print_number(-n)) */
	/* } */

	/* if (n > 9) */
	/* { */
	/* 	return (1 + print_number(n / 10)); */
	/* } */

	return (n);
}
